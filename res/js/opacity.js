function mostrar(nombreElemento){	var elemento = document.getElementById(nombreElemento);	if (elemento)	{		if (elemento.style.opacity < 1)		{			try			{				elemento.style.opacity = parseFloat(elemento.style.opacity) + 0.05;				elemento.filters.alpha.opacity = parseInt(elemento.filters.alpha.opacity) + 5;			}			catch (E)			{}			setTimeout("mostrar('" + nombreElemento + "');", 200);		}	}}function ocultar(nombreElemento){	var elemento = document.getElementById(nombreElemento);	if (elemento)	{		if (elemento.style.opacity > 0)		{			try			{				elemento.style.opacity = parseFloat(elemento.style.opacity) - 0.05;				elemento.filters.alpha.opacity = parseInt(elemento.filters.alpha.opacity) - 5;			}			catch (E)			{}			setTimeout("ocultar('" + nombreElemento + "');", 200);		}	}}