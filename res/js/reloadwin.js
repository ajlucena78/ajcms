function reloadwin()
{
	/*
	var contenido = document.getElementById("content-wrapper");
	if (contenido)
		contenido.style.width = (getWindowWidth() - 310) + "px";
	var contDivImg = 1;
	while (true)
	{
		var imagenes = document.getElementById("imagenes_" + contDivImg);
		if (!imagenes)
			break;
		var nImagenes = document.getElementById("nImagenes_" + contDivImg);
		alert(nImagenes.value);
		var tam = nImagenes.value * 220;
		if (tam < (getWindowWidth() - 310))
			imagenes.style.width = tam + "px";
		else
		{
			var res = Math.floor((getWindowWidth() - 310) / 220);
			tam = Math.round(res * 220);
			imagenes.style.width = tam + "px";
		}
		contDivImg++;
	}
	*/
}